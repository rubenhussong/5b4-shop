import { RouteScrollStateDirective } from './route-scroll-state.directive';

describe('RouteScrollStateDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteScrollStateDirective();
    expect(directive).toBeTruthy();
  });
});
